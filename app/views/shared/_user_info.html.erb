<%= link_to gravatar_for(@user, size: 50), @user %>
<h1><%= @user.name %></h1>
<% if current_user == @user %>
  <span><%= link_to "マイページ", @user %></span>
<% else %>
  <span><%= link_to "#{ @user.name }さんのゴールへ", @user %></span>
<% end %>
<span><%= pluralize(@user.posts.count, "投稿") %></span>